export enum UserStatus {
  INITIAL = "INITIAL",
  PENDING = "PENDING",
  CONFIRMED = "CONFIRMED",
  REJECTED = "REJECTED",
}

export enum UserRole {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum TaskStatus {
  ACTIVE = "ACTIVE",
  URGENT = "URGENT",
  PAST_DUE = "PAST DUE",
  COMPLETE = "COMPLETE",
  DISCARDED = "DISCARDED",
}
